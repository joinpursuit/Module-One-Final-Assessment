/**
* Removes all vowels from an input string. 
* For this problem, treat y as a consonant, not a vowel.
* Vowels are "a", "e", "i", "o", and "u" (upper and lowercase)
*
* @param {string} str - The input string
* @returns {string} - Returns a new string without any vowels.
*
* ex: removeVowels("HELLO")
* returns: "HLL"
*
* ex: removeVowels("Sunny")
* returns: "Snny"
*
*/

function removeVowels(string) {

let str = "";

 
for (i = 0; i < str.length; i++) { //iterate thorugh string
if(str[i] === 'a' || str[i] === 'e'|| str[i] === 'i' || str[i] === 'o'|| str[i] === 'u') { //if the string indexes equal AEIOU, keep
    //if sting length equals to aeious remove them
continue;
}
result+=S[i];

}

return SpeechRecognitionResult;


}

module.exports = removeVowels
